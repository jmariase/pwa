apiVersion: v1
kind: Service
metadata:
  name: pwa-service
  labels:
    app: pwa-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: pwa-app
